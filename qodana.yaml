version: "1.0"

# Inspection profile used for analyzing the code
profile:
  name: qodana.recommended

# Project SDK version
projectJDK: 21

# Qodana linter used for analyzing the code
linter: jetbrains/qodana-jvm:latest
